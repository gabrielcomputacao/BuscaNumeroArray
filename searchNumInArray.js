export function findNumberTarget(array, target) {

    const targetInteger = parseInt(target)

    if (array.length > 0) {
        array.sort((a, b) => a - b)

        console.log(array)



    } else {
        return 0
    }








}